package com.feywild.quest_giver.quest.util;

public enum SpecialTaskaction {

    /* SPECIAL TASKS CAN BE ADDED HERE - NOTE ALL ENUMS MUST BE UPPERCASE
    * Example:  QuestData.get((ServerPlayer) player).checkComplete(SpecialTask.INSTANCE, SpecialTaskAction.LEVITATE_SHEEP);
    * Enum would be: LEVITATE_SHEEP */

}
